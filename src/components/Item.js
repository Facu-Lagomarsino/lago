function Item({name, description}) {

    return(
        <>
            <p>{description}</p>
            <span>{name}</span>
            <h1>FOTO!</h1>
            <button>AGREGAR</button>
        </>
    )
} 

export default Item;